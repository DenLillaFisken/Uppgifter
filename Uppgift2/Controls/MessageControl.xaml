<UserControl x:Class="Uppgift2.Controls.MessageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uppgift2.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="160">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="center" Margin="10,0,0,0">
            <TextBlock x:Name="contactName"  Margin="0,0,0,10" />
            <TextBlock x:Name="message" Grid.Row="1" FontSize="8" Foreground="Black"/>
        </StackPanel>
        <StackPanel Grid.Column="1" VerticalAlignment="center" Margin="10,0,0,0">
            <TextBlock x:Name="date" Grid.Row="0" Margin="0,0,0,10" />
            <TextBlock x:Name="time" Grid.Row="1" FontSize="8" Foreground="Black"/>
        </StackPanel>
    </Grid>
</UserControl>
